<!DOCTYPE html>
<html lang="ja" class="dark">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= content_for(:title) || "読書感想文" %></title>
  <meta name="description" content="<%= content_for(:description) || "読書感想を投稿・共有できるソーシャルプラットフォーム" %>">

  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
</head>

<body class="bg-bg-primary text-primary min-h-screen flex flex-col" data-controller="dark-mode">
  <%= render "layouts/navbar" %>

  <main class="flex-1">
    <div class="container mx-auto px-4 py-6 max-w-7xl">
      <%= render "layouts/flash" %>
      <%= yield %>
    </div>
  </main>

  <%= render "layouts/footer" %>

  <!-- Toast Container -->
  <div id="toast-container" class="fixed bottom-4 right-4 z-50 space-y-2"></div>
</body>
</html>
